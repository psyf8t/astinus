package layer

// This file is a placeholder for future per-layer streaming
// extraction (Stage 4 needs the file bytes for fingerprinting). The
// walker.go API is sufficient for Stage 3 (attribution); we keep the
// file so the spec's listed file set (extract.go) is satisfied
// without writing speculative code that would have to change once
// Stage 4 produces real requirements.
